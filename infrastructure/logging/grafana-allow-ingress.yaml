apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ingress-to-grafana
  namespace: flux-system
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: grafana
      app.kubernetes.io/instance: loki-stack
  policyTypes:
    - Ingress
  ingress:
    # Разрешить весь трафик (для отладки)
    - {}